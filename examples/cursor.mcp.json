{
  "mcpServers": {
    "z-image": {
      "command": "npx",
      "args": [
        "-y",
        "@runapi.ai/z-image-mcp"
      ],
      "env": {
        "RUNAPI_API_KEY": "${RUNAPI_API_KEY}"
      }
    }
  }
}
